# Module 1: Data Fundamentals Assignment

This comprehensive assignment tests your understanding of data analytics, Python programming, statistics, NumPy operations, and Pandas data analysis. Answer all multiple-choice questions.

## Part 1: Introduction to Data Analytics (20 points)

1. What is the primary purpose of data collection?
   - a. To analyze data
   - b. To gather raw material for analysis
   - c. To ensure data accuracy
   - d. To visualize data

2. Which of the following is an example of first-party data?
   - a. Social media insights
   - b. Market research reports
   - c. Customer interactions
   - d. Data aggregators

3. What does GDPR stand for?
   - a. General Data Protection Regulation
   - b. General Data Privacy Regulation
   - c. General Data Protection Regulation
   - d. Global Data Protection Regulation

4. Which method is used for gathering qualitative data?
   - a. Surveys
   - b. Data logging
   - c. Interviews
   - d. Web scraping

5. What is the main focus of predictive analytics?
   - a. Understanding past events
   - b. Analyzing current data
   - c. Forecasting future outcomes
   - d. Summarizing data

6. What is data cleaning?
   - a. Organizing data in a specific format
   - b. Removing duplicate entries
   - c. Correcting errors and inconsistencies
   - d. All of the above

7. Which type of data is collected through direct observation?
   - a. Primary data
   - b. Secondary data
   - c. Tertiary data
   - d. Meta data

8. What is the purpose of data visualization?
   - a. To clean data
   - b. To present data in a graphical format
   - c. To collect data
   - d. To store data

9. Which of these is a characteristic of big data?
   - a. Volume
   - b. Velocity
   - c. Variety
   - d. All of the above

10. What is data mining?
    - a. Collecting raw data
    - b. Discovering patterns in large datasets
    - c. Storing data in databases
    - d. Creating data backups

11. Which of these is not a type of data analytics?
    - a. Descriptive analytics
    - b. Predictive analytics
    - c. Prescriptive analytics
    - d. Restrictive analytics

12. What is the role of a data analyst?
    - a. Only collecting data
    - b. Only cleaning data
    - c. Only visualizing data
    - d. Interpreting data to support decision-making

13. Which of these is an example of structured data?
    - a. Email messages
    - b. Social media posts
    - c. Database tables
    - d. Video files

14. What is data governance?
    - a. Data collection methods
    - b. Data storage solutions
    - c. Framework for data management
    - d. Data analysis techniques

15. Which factor is most important in ensuring data quality?
    - a. Data volume
    - b. Data accuracy
    - c. Data age
    - d. Data source

16. What is metadata?
    - a. Raw data
    - b. Processed data
    - c. Data about data
    - d. Big data

17. Which is not a common data collection method?
    - a. Surveys
    - b. Interviews
    - c. Telepathy
    - d. Observations

18. What is the purpose of data aggregation?
    - a. To collect data
    - b. To combine data from multiple sources
    - c. To delete data
    - d. To encrypt data

19. Which is a benefit of real-time data analytics?
    - a. Lower costs
    - b. Immediate insights
    - c. Simpler analysis
    - d. Reduced data volume

20. What is data integrity?
    - a. Data encryption
    - b. Data backup
    - c. Data accuracy and consistency
    - d. Data compression

## Part 2: Python Programming (20 points)

1. Which of the following correctly declares a list in Python?
   - a. list = (1, 2, 3)
   - b. list = [1, 2, 3]
   - c. list = {1, 2, 3}
   - d. list = <1, 2, 3>

2. What is the output of len(['a', 'b', ['c', 'd']])?
   - a. 2
   - b. 3
   - c. 4
   - d. Error

3. Which statement correctly creates a dictionary?
   - a. dict = ['name': 'John', 'age': 30]
   - b. dict = {'name': 'John', 'age': 30}
   - c. dict = ('name': 'John', 'age': 30)
   - d. dict = <'name': 'John', 'age': 30>

4. What is the result of 3 * 'abc'?
   - a. 'abc3'
   - b. '3abc'
   - c. 'abcabcabc'
   - d. Error

5. Which method is used to add an item to a list?
   - a. add()
   - b. append()
   - c. insert()
   - d. extend()

6. What is the output of 'Hello' + 'World'?
   - a. HelloWorld
   - b. Hello World
   - c. HELLOWORLD
   - d. Error

7. Which operator is used for exponentiation in Python?
   - a. ^
   - b. **
   - c. ^^
   - d. //

8. What is the result of bool(0)?
   - a. 0
   - b. 1
   - c. True
   - d. False

9. How do you convert a string to lowercase in Python?
   - a. str.lower()
   - b. str.lowercase()
   - c. str.toLower()
   - d. lowercase(str)

10. What does the split() method do?
    - a. Divides a number
    - b. Breaks a string into a list
    - c. Separates a list
    - d. Splits a dictionary

11. Which method removes whitespace from both ends of a string?
    - a. trim()
    - b. strip()
    - c. remove()
    - d. clean()

12. What is the correct way to create a function in Python?
    - a. function myFunc():
    - b. def myFunc():
    - c. create myFunc():
    - d. func myFunc():

13. How do you get the length of a tuple?
    - a. tuple.length()
    - b. tuple.size()
    - c. len(tuple)
    - d. size(tuple)

14. What is the correct way to import a module in Python?
    - a. #include module
    - b. import module
    - c. using module
    - d. require module

15. Which method adds an element at the end of a list?
    - a. push()
    - b. append()
    - c. add()
    - d. insert()

16. What is the output of type([1, 2, 3])?
    - a. array
    - b. list
    - c. tuple
    - d. set

17. How do you create an empty dictionary?
    - a. dict()
    - b. {}
    - c. Both a and b
    - d. None of the above

18. What is the result of 10 / 3?
    - a. 3
    - b. 3.0
    - c. 3.33
    - d. 3.333333...

19. Which of these is not a valid variable name?
    - a. my_var
    - b. _myvar
    - c. 1var
    - d. myVar

20. What is the output of 'python'[1:4]?
    - a. pyth
    - b. yth
    - c. ytho
    - d. python

## Part 3: Statistics (20 points)

1. What is the mean of the numbers 2, 4, 6, 8, 10?
   - a. 5
   - b. 6
   - c. 7
   - d. 8

2. Which measure of central tendency is most affected by outliers?
   - a. Mean
   - b. Median
   - c. Mode
   - d. Range

3. What is the probability of rolling a 6 on a fair six-sided die?
   - a. 1/2
   - b. 1/3
   - c. 1/4
   - d. 1/6

4. Which distribution is symmetric and bell-shaped?
   - a. Uniform
   - b. Normal
   - c. Exponential
   - d. Binomial

5. What is the standard deviation a measure of?
   - a. Central tendency
   - b. Variability
   - c. Position
   - d. Shape

6. What is the median of the numbers 1, 3, 3, 6, 7, 8, 9?
   - a. 3
   - b. 6
   - c. 7
   - d. 8

7. Which type of correlation indicates that as one variable increases, the other decreases?
   - a. Positive
   - b. Negative
   - c. Zero
   - d. Perfect

8. What is the range of possible correlation coefficient values?
   - a. 0 to 1
   - b. -1 to 0
   - c. -1 to 1
   - d. -∞ to ∞

9. Which measure is resistant to outliers?
   - a. Mean
   - b. Range
   - c. Median
   - d. Variance

10. What is the mode of the numbers 2, 2, 3, 4, 4, 4, 5?
    - a. 2
    - b. 3
    - c. 4
    - d. 5

11. What is the probability of getting heads on a fair coin flip?
    - a. 0
    - b. 1/4
    - c. 1/2
    - d. 1

12. Which measure of spread is always positive?
    - a. Mean
    - b. Median
    - c. Variance
    - d. Correlation

13. What is the interquartile range (IQR)?
    - a. Difference between largest and smallest values
    - b. Difference between Q3 and Q1
    - c. Difference between mean and median
    - d. Difference between mode and mean

14. Which type of variable is age?
    - a. Nominal
    - b. Ordinal
    - c. Interval
    - d. Ratio

15. What is a percentile?
    - a. A measure of central tendency
    - b. A measure of spread
    - c. A measure of relative position
    - d. A measure of correlation

16. Which distribution is appropriate for count data?
    - a. Normal
    - b. Poisson
    - c. Uniform
    - d. Exponential

17. What is the law of large numbers about?
    - a. Sample size effect on variance
    - b. Population parameters
    - c. Sample mean convergence
    - d. Data distribution

18. Which test is used to compare two population means?
    - a. Chi-square test
    - b. F-test
    - c. t-test
    - d. z-test

19. What is a Type I error?
    - a. Failing to reject a false null hypothesis
    - b. Rejecting a true null hypothesis
    - c. Accepting a false alternative hypothesis
    - d. Rejecting a false alternative hypothesis

20. What is the purpose of a confidence interval?
    - a. To test hypotheses
    - b. To estimate parameters
    - c. To measure correlation
    - d. To calculate probability

## Part 4: NumPy Operations (20 points)

1. Which NumPy function creates an array filled with zeros?
   - a. np.zeros()
   - b. np.empty()
   - c. np.null()
   - d. np.blank()

2. How do you find the shape of a NumPy array?
   - a. array.dimensions
   - b. array.shape
   - c. array.size
   - d. array.length

3. Which function is used for matrix multiplication in NumPy?
   - a. np.multiply()
   - b. np.matmul()
   - c. np.dot()
   - d. Both b and c are correct

4. How do you create an array of evenly spaced numbers in NumPy?
   - a. np.range()
   - b. np.linspace()
   - c. np.arange()
   - d. np.space()

5. Which axis represents columns in a 2D NumPy array?
   - a. axis=0
   - b. axis=1
   - c. axis=2
   - d. axis=-1

6. What is the purpose of np.array()?
   - a. To create a list
   - b. To create an array
   - c. To convert to array
   - d. Both b and c

7. How do you check the number of dimensions in a NumPy array?
   - a. array.dim
   - b. array.ndim
   - c. array.dimensions
   - d. array.shape

8. Which function is used to generate random numbers in NumPy?
   - a. np.random()
   - b. np.rand()
   - c. np.random.rand()
   - d. np.generate()

9. How do you reshape a NumPy array?
   - a. array.reshape()
   - b. np.reshape()
   - c. array.shape()
   - d. np.reform()

10. What does np.ones() create?
    - a. Array of zeros
    - b. Array of ones
    - c. Identity matrix
    - d. Empty array

11. How do you find the mean of a NumPy array?
    - a. array.mean()
    - b. np.mean(array)
    - c. Both a and b
    - d. mean(array)

12. What is broadcasting in NumPy?
    - a. Array multiplication
    - b. Array addition
    - c. Array operation rules
    - d. Array division

13. How do you stack arrays vertically in NumPy?
    - a. np.hstack()
    - b. np.vstack()
    - c. np.dstack()
    - d. np.stack()

14. What does np.arange() do?
    - a. Creates array of random numbers
    - b. Creates array of evenly spaced numbers
    - c. Creates array of ones
    - d. Creates empty array

15. How do you find the maximum value in a NumPy array?
    - a. array.max()
    - b. np.max(array)
    - c. Both a and b
    - d. maximum(array)

16. What is the purpose of np.eye()?
    - a. Creates zero matrix
    - b. Creates identity matrix
    - c. Creates ones matrix
    - d. Creates random matrix

17. How do you compute the dot product?
    - a. np.dot()
    - b. array.dot()
    - c. Both a and b
    - d. np.product()

18. What does np.unique() do?
    - a. Sorts array
    - b. Finds unique elements
    - c. Removes duplicates
    - d. All of the above

19. How do you transpose a matrix?
    - a. array.T
    - b. np.transpose()
    - c. Both a and b
    - d. array.transpose()

20. What is the purpose of np.where()?
    - a. Finds indices
    - b. Conditional selection
    - c. Both a and b
    - d. Searches array

## Part 5: Pandas Data Analysis (20 points)

1. Which method is used to handle missing values in a DataFrame?
   - a. df.remove()
   - b. df.dropna()
   - c. df.delete()
   - d. df.clean()

2. How do you select a single column in a DataFrame?
   - a. df.column
   - b. df[column]
   - c. df.get(column)
   - d. df.select(column)

3. Which method is used to merge two DataFrames?
   - a. df1.combine(df2)
   - b. df1.join(df2)
   - c. df1.merge(df2)
   - d. df1.concat(df2)

4. How do you group data in a DataFrame?
   - a. df.group()
   - b. df.groupby()
   - c. df.aggregate()
   - d. df.cluster()

5. Which method is used to sort a DataFrame by values?
   - a. df.order()
   - b. df.sort()
   - c. df.sort_values()
   - d. df.arrange()

6. How do you read a CSV file in Pandas?
   - a. pd.read_csv()
   - b. pd.load_csv()
   - c. pd.import_csv()
   - d. pd.open_csv()

7. What is the purpose of df.head()?
   - a. Shows first row
   - b. Shows first 5 rows
   - c. Shows column headers
   - d. Shows data types

8. How do you select multiple columns in a DataFrame?
   - a. df[['col1', 'col2']]
   - b. df['col1', 'col2']
   - c. df.select(['col1', 'col2'])
   - d. df.get(['col1', 'col2'])

9. What does df.describe() do?
   - a. Shows data types
   - b. Shows missing values
   - c. Shows statistical summary
   - d. Shows column names

10. How do you rename columns in a DataFrame?
    - a. df.rename()
    - b. df.columns()
    - c. df.change()
    - d. df.modify()

11. What is the purpose of df.fillna()?
    - a. Removes missing values
    - b. Replaces missing values
    - c. Counts missing values
    - d. Finds missing values

12. How do you add a new column to a DataFrame?
    - a. df.add_column()
    - b. df.insert()
    - c. df['new_col'] = values
    - d. df.append()

13. What does df.info() display?
    - a. Data types and non-null values
    - b. Statistical summary
    - c. First few rows
    - d. Column names only

14. How do you select rows based on a condition?
    - a. df.select()
    - b. df.filter()
    - c. df[condition]
    - d. df.where()

15. What is the purpose of df.value_counts()?
    - a. Counts unique values
    - b. Counts all values
    - c. Counts missing values
    - d. Counts columns

16. How do you save a DataFrame to a CSV file?
    - a. df.save()
    - b. df.to_csv()
    - c. df.write_csv()
    - d. df.export()

17. What does df.shape return?
    - a. Column names
    - b. Data types
    - c. Dimensions
    - d. Memory usage

18. How do you sort a DataFrame by index?
    - a. df.sort()
    - b. df.sort_index()
    - c. df.index_sort()
    - d. df.order()

19. What is the purpose of df.apply()?
    - a. Applies function to elements
    - b. Applies formatting
    - c. Applies sorting
    - d. Applies filtering

20. How do you get basic information about a DataFrame?
    - a. df.information()
    - b. df.info()
    - c. df.describe()
    - d. df.summary()

## Grading Criteria

Each part is worth 20 points:
- 1 point per correct answer (20 questions per section)
- Total possible points: 100

## Submission Guidelines

1. Submit your answers in a clear format, indicating the question number and your chosen answer (a, b, c, or d).
2. Double-check your answers before submission.
3. Make sure to answer all questions - unanswered questions will be marked as incorrect.
