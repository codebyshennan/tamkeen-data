# **Assignment: Introduction to Python**

## **Task 1: Data Types and Variables**

```python
# Integers
num = 42
print(f"Integer: {num}")

# Floats
pi = 3.14159
print(f"Float: {pi}")

# Strings
message = "Hello Python!"
print(f"String: {message}")

# Boolean
is_active = True
print(f"Boolean: {is_active}")

# Lists
fruits = ["apple", "banana", "orange"]
print(f"List: {fruits}")

# Tuples
coordinates = (10, 20)
print(f"Tuple: {coordinates}")

# Dictionaries
person = {
    "name": "John",
    "age": 30,
    "city": "New York"
}
print(f"Dictionary: {person}")
```

## **Task 2: Functions and Classes**

```python
def count_and_return_vowels(text):
    vowels = 'aeiouAEIOU'
    found_vowels = [char for char in text if char in vowels]
    return len(found_vowels), found_vowels

def sum_of_even_numbers(limit):
    total = 0
    num = 0
    while num <= limit:
        if num % 2 == 0:
            total += num
        num += 1
    return total

class BankAccount:
    def __init__(self, initial_balance):
        self.balance = initial_balance
        
    def deposit(self, amount):
        self.balance += amount
        
    def withdraw(self, amount):
        if amount <= self.balance:
            self.balance -= amount
        else:
            print("Insufficient funds")
            
    def get_balance(self):
        return self.balance

# Test the code
print("Testing count_and_return_vowels:")
print(count_and_return_vowels("Hello World"))
print(count_and_return_vowels("Programming"))
print(count_and_return_vowels("OpenAI"))

print("\nTesting sum_of_even_numbers:")
print(sum_of_even_numbers(10))
print(sum_of_even_numbers(5))
print(sum_of_even_numbers(1))

print("\nTesting BankAccount:")
account = BankAccount(100)
print(account.get_balance())
account.deposit(50)
print(account.get_balance())
account.withdraw(30)
print(account.get_balance())
account.withdraw(200)
print(account.get_balance())
```
